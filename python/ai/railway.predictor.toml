[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn predictor:app --host 0.0.0.0 --port 5010"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
